@using Microsoft.Extensions.DependencyInjection;
@inject System.IServiceProvider _serviceProvider;

@code {
    protected override Task OnAfterRenderAsync(bool firstRender)
    {
        var masterComponent = _serviceProvider.GetService<test_app.Components.Master>();
        return masterComponent.RenderAsync(Guid.Empty, true).AsTask();
    }
}